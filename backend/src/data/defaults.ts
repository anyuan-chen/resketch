// if ocr has >= this threshold, that player wins
export const WIN_CONFIDENCE_THRESHOLD = 0.65;

export const NUM_ROUNDS = 3;

export const MAX_DRAWS_BEFORE_SYNC = 5;

export const SYNC_INTERVAL_MS = 1000;
